<UserControl x:Class="PraxisWpf.Features.TimeTracker.TimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PraxisWpf.Features.TimeTracker"
             xmlns:models="clr-namespace:PraxisWpf.Models"
             Focusable="True" TabIndex="0">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with week navigation -->
        <Border Grid.Row="0" Background="{DynamicResource CyberControlBrush}" 
                BorderBrush="{DynamicResource CyberBorderBrush}" 
                BorderThickness="0,0,0,2" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="1" 
                           Text="{Binding WeekStartDate, StringFormat='╔═══ TIME ENTRY - Week of {0:MMM dd, yyyy} ═══╗'}" 
                           Style="{DynamicResource CyberHeading1}"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                
                <TextBlock Grid.Column="4" 
                           Text="{Binding WeekTotal, StringFormat='Week Total: {0:F2}h'}" 
                           Style="{DynamicResource CyberHeading3}"
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Inline Time Entry -->
        <Border Grid.Row="1" Background="{DynamicResource CyberControlBrush}" 
                BorderBrush="{DynamicResource CyberBorderBrush}" 
                BorderThickness="0,0,0,1" Padding="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Date Display -->
                <TextBlock Grid.Row="0" 
                           Text="{Binding SelectedDate, StringFormat='Selected Date: {0:ddd, MMM dd, yyyy}'}" 
                           Style="{DynamicResource CyberHeading3}"
                           HorizontalAlignment="Center" Margin="0,0,0,10"/>
                
                <!-- Inline Entry Controls -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="80"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Project Selection -->
                    <ComboBox Grid.Column="0" 
                              ItemsSource="{Binding AvailableProjects}" 
                              SelectedItem="{Binding SelectedProject}"
                              DisplayMemberPath="Name"
                              TabIndex="1"
                              IsTabStop="True"
                              Margin="0,0,5,0">
                    </ComboBox>
                    
                    <!-- Hours Entry -->
                    <ComboBox Grid.Column="1" 
                              ItemsSource="{Binding HourIncrements}" 
                              SelectedItem="{Binding EntryHours}"
                              TabIndex="2"
                              IsTabStop="True"
                              Margin="0,0,5,0"/>
                    
                    <!-- Description Entry -->
                    <TextBox Grid.Column="2" 
                             Text="{Binding EntryDescription, UpdateSourceTrigger=PropertyChanged}"
                             TabIndex="3"
                             IsTabStop="True"
                             Margin="0,0,5,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding AddProjectTimeCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    
                    <!-- Add Project Entry -->
                    <TextBlock Grid.Column="3" Text="[P]roject" 
                               Style="{DynamicResource CyberLabel}"
                               Margin="0,0,10,0"/>
                    
                    <!-- Add Generic Entry -->
                    <TextBlock Grid.Column="4" Text="[N]ew Generic" 
                               Style="{DynamicResource CyberLabel}"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Week view grid -->
        <Border Grid.Row="2" BorderBrush="{DynamicResource CyberBorderBrush}" 
                BorderThickness="1" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Week header -->
                <Border Grid.Row="0" Background="{DynamicResource CyberControlBrush}" 
                        BorderBrush="{DynamicResource CyberBorderBrush}" 
                        BorderThickness="0,0,0,1" Padding="5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="60"/>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="║ Project" 
                                   Style="{DynamicResource CyberLabel}"/>
                        <TextBlock Grid.Column="1" Text="║ Description" 
                                   Style="{DynamicResource CyberLabel}"/>
                        <TextBlock Grid.Column="2" Text="║ Hours" 
                                   Style="{DynamicResource CyberLabel}"/>
                        <TextBlock Grid.Column="3" Text="║ Date" 
                                   Style="{DynamicResource CyberLabel}"/>
                        <TextBlock Grid.Column="4" Text="║ Day Total" 
                                   Style="{DynamicResource CyberLabel}"/>
                    </Grid>
                </Border>

                <!-- Time entries list -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding CurrentWeekEntries}" 
                          SelectedItem="{Binding SelectedTimeEntry}"
                          AutoGenerateColumns="False" CanUserAddRows="False" 
                          CanUserDeleteRows="False" CanUserResizeRows="False"
                          GridLinesVisibility="Horizontal" HeadersVisibility="None"
                          Background="{DynamicResource CyberBackgroundBrush}"
                          RowBackground="{DynamicResource CyberBackgroundBrush}"
                          AlternatingRowBackground="{DynamicResource CyberControlBrush}">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding ProjectReference}" Width="80" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource CyberAccentBrush}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Padding" Value="4,2"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Binding="{Binding Description}" Width="*" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource CyberAccentBrush}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Padding" Value="4,2"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Binding="{Binding Hours, StringFormat=F2}" Width="60" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource CyberWarningBrush}"/>
                                    <Setter Property="Padding" Value="4,2"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Binding="{Binding Date, StringFormat=ddd MM/dd}" Width="80" IsReadOnly="True">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{DynamicResource CyberAccentBrush}"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Padding" Value="4,2"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Foreground="{DynamicResource CyberInfoBrush}" 
                                               Padding="4,2" FontStyle="Italic">
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="Day: {0:F2}h">
                                                <Binding Path="DataContext.DayTotal" 
                                                         RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

    </Grid>
</UserControl>